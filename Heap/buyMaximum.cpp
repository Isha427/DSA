#include <bits/stdc++.h>
using namespace std;
void sum(int arr[],int n,int k)
{
       priority_queue<int,vector<int>,greater<int>>pq(arr,arr+n);

       while(pq.top()<=sum)
       {
              c++;
              k-=
              pq.pop();

       }
}
int main(){
  
  return 0;
}
